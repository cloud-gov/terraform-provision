---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: python
    tag: '3.10'

inputs:
  - name: cg-provision-repo
  - name: waf-test-files

run:
  path: cg-provision-repo/ci/waf-tests/test-waf-rules.sh

params:
  WAF_TEST_APP_URL: